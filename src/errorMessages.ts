export const errorMessage = {
  useFormResultChanged: `useFormResult prop changed - its value shouldn't changed during the lifetime of the component.`,
  isFunctionChanged:
    "props should either be a function or not a function for the entire lifetime of your form component.",
};
