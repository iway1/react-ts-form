# things to make sure that work

- optional types should have same props as their non optional counterparts
- a schema props shouldn't allow a unique field schemas props (or other potential schema props)
